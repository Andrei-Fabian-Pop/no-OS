ret = no_os_irq_set_priority(<%= it.symbol_name %>.irq_ctrl, <%= it.symbol_name %>.irq_id, 1);
if (ret)
	return ret;

ret = iio_hw_trig_init(&desc.<%= it.descriptor_name %>, &<%= it.symbol_name %>);
if (ret)
	return ret;
