$id: "platforms/stm32/stm32_dma_channel.yaml"
$type: struct
$symbol: stm32_dma_channel
$description: "STM32 DMA channel configuration"
$ranking: 4
$sources:
  platform:
    - "stm32_dma.h"

# FIXME: This is not valid ('external_ptr' type unsupported by schema tooling)
# Evidence: stm32_dma.c:64/66 dereferences sdma_ch->hdma->Init/Instance and :130
# HAL_DMA_Init(sdma_ch->hdma) unconditionally (no NULL check on hdma) -> hdma is
# MANDATORY when a DMA channel is used.
# When the external_ptr type is supported, add: required: true
# hdma:
#   type: external_ptr
#   target: DMA_HandleTypeDef
#   required: true
#   description: "DMA handle (HAL type, managed by STM32CubeMX)"

ch_num:
  type: uint32_t
  description: "Channel number"

mem_increment:
  type: bool
  default: true
  description: "Memory address increment enable"

per_increment:
  type: bool
  default: false
  description: "Peripheral address increment enable"

mem_data_alignment:
  include: "platforms/stm32/enums/stm32_dma_data_alignment.yaml"
  description: "Memory data alignment"

per_data_alignment:
  include: "platforms/stm32/enums/stm32_dma_data_alignment.yaml"
  description: "Peripheral data alignment"

dma_mode:
  include: "platforms/stm32/enums/stm32_dma_mode.yaml"
  description: "DMA mode"
